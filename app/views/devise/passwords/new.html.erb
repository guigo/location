<div>
  <h2>RECUPERAR SENHA</h2>
  <br>
  <p>Nós enviaremos as instruções de recuperação de senha para o email cadastrado.</p>
</div>
<br>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.email_field :email, class: "input", placeholder: "Email", autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field is-centered">
    <div class="actions">
      <%= f.submit "ENVIAR", class: 'button is-fullwidth is-medium is-primary' %>
    </div>
  </div>
<% end %>

<%= render "devise/shared/links" %>
